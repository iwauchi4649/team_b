-#ユーザーがサインインしているかどうか
- if user_signed_in?
  -#ログインしているユーザーがすでに「いいね」しているかどうか
  - if good.like_user(current_user.id)
    .item-button-container__left__dislike
      =link_to user_session_path(current_user, good, like), method: "DELETE", remote: true do
        %i.fas.fa-heart
        %span いいね！
        %span
          = good.likes_count
  - else
    .item-button-container__left__like
      =link_to user_session_path(current_user, good), method: "POST", remote: true do
        %i.fas.fa-heart
        %span いいね！
        %span
          = good.likes_count
-else
  .item-button-container__left__like
    %i.fas.fa-heart
      %span いいね！
      %span = good.like_count
