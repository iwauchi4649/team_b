.wrapper
  .header
    = render './review/top-header'
    .header__signup-bar

  .main
    .main__container
      .main__container__head お届け先住所入力
      .main__container__content
        .main__container__content__form
          = form_with model: @address, local: true do |f|
            .form-group
              = f.label :'郵便番号'
              %span.form-required  必須
              = f.text_field :potal_code, autofocus: true, placeholder: "例)123-4567", class: 'address-input-form'
              %ul.error
                %li#post-error.error__text
                  郵便番号を入力してください
            .form-group
              = f.label '都道府県'
              %span.form-required  必須
              .prefectures-select
                .select-wrap
                  = f.select :prefectures, Address.prefectures.keys, {}, {class: 'select-default'}
              %ul.error
                %li#prefectures-error.error__text
                  都道府県を入力してください
            .form-group
              = f.label '市町村'
              %span.form-required  必須
              = f.text_field :municipalties, autofocus: true, placeholder: "例)横浜市緑区", class: 'address-input-form'
              %ul.error
                %li#city-error.error__text
                  市町村区を入力してください
            .form-group
              = f.label '番地'
              %span.form-required  必須
              = f.text_field :banchi, autofocus: true, placeholder: "例)青山1-1-1", class: 'address-input-form'
              %ul.error
                %li#address-error.error__text
                  番地を入力してください
            .form-group
              = f.label '建物名'
              %span.form-free  任意
              = f.text_field :buildname, autofocus: true, placeholder: "例)柳ビル103", class: 'address-input-form'
              %ul.error
                %li#building-error.error__text
                  建物名を入力してください
            .form-info-text
              ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります
            .low__content
              %p.low__content__text「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします
            = f.submit '次へ進む', class: 'btn-default'
  .top-footer
    = render './review/top-footer'