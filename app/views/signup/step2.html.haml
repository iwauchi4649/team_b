= form_with model: @user,url: step3_signup_index_path, method: :get,local: true do |f|
  .field
    = f.label :call_number
    %br/
    = f.text_field ,:call_number, autofocus: true, autocomplete: "call_number"
  = f.submit 'continue'